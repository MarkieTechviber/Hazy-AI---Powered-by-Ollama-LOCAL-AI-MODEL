{
  "name": "hazy-server",
  "version": "1.0.0",
  "description": "Hazy local companion backend - Hazy AI - Ollama proxy + static server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --require ./tests/testSetup.cjs --test --test-timeout=30000 tests/*.test.js",
    "test:integration:ollama": "node --test --test-timeout=180000 integration/ollama.integration.test.js"
  },
  "keywords": [
    "companion",
    "support",
    "ollama",
    "llm"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=22.13.0"
  },
  "dependencies": {
    "playwright": "^1.61.1"
  }
}
